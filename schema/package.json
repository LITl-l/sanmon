{
  "name": "@sanmon/schema",
  "version": "0.1.0",
  "description": "Action type definitions and JSON Schema generation for sanmon",
  "private": true,
  "scripts": {
    "build": "tsc",
    "generate-schema": "node --experimental-strip-types src/generate.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "zod": "^3.24.0",
    "zod-to-json-schema": "^3.24.0"
  }
}
